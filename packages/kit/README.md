# @iconic/kit

The collection-authoring toolkit (`defineIconicPreset`). Bundles a curated set
of Iconify icons plus normalization into a reusable preset that apps can
include wholesale — the basis for the reserved `presets/*` bucket.

> Scaffold — implementation pending.
