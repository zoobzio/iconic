# @iconic/utils

Structural operations over icon collections — `merge` sets, `subset` to a used
list, `prune` unreferenced aliases, `diff` two collections. Pure data
transforms, no rendering.

> Scaffold — implementation pending.
