# @iconic/svg

The SVG sprite-sheet renderer (`defineSprite`). Consumes a resolved iconic
catalog and emits a single `<svg>` sprite of `<symbol>` definitions, plus the
`<use href="#name">` references and optimized inline SVG for individual icons.

> Scaffold — implementation pending.
