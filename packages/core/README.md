# @iconic/core

The icon engine (`defineIconic`). Loads one or more Iconify JSON collections,
resolves an icon name through its aliases and inherited set-wide transforms
into a normalized symbol (`{ body, width, height, viewBox }`), and subsets the
catalog to the icons an app actually includes.

> Scaffold — implementation pending.
