// Typecheck-only stub for the generated `#build/types/iconic.d.ts` virtual module.
import type { IconifyIcon } from "@iconic/iconic";

export type Alias = "home" | "save";

export type Overrides = Partial<Record<Alias, IconifyIcon>>;
